<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.duoku.platform.demo.canvassample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".BaisaierActivity">
        </activity>
        <activity android:name=".BeisaierActivity"/>
        <activity android:name=".RobotActivity"/>
        <activity android:name=".TestAactivity"/>
        <activity android:name=".BaseActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".Animation_Shape"
            android:label="@string/title_activity_animation__shape"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".SearchViewActivity"/>
        <activity android:name=".ClockActivity"/>
        <activity android:name=".SvgViewActivity">
        </activity>
    </application>

</manifest>